name: panda_printer_example
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_blue_plus: ^1.32.8
  print_plugin:
    path: plugins/print_plugin
  dartz: ^0.10.1
  permission_handler: ^11.3.1
  connectivity_plus: ^5.0.2
  panda_print_plugin:
    path: plugins/panda_print_plugin

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  #
